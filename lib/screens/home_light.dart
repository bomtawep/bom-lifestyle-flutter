import 'package:flutter/material.dart';

class HomeLight extends StatelessWidget {
  const HomeLight({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Home Light'),
      ),
    );
  }
}