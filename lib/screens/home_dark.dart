import 'package:flutter/material.dart';

class HomeDark extends StatelessWidget {
  const HomeDark({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Home Dark'),
      ),
    );
  }
}